[build]
  command = "npm install && npm run build"
  publish = ".next"

[build.environment]
  NODE_VERSION = "18"
  GO_VERSION = "1.19"

[context.production.environment]
  NODE_VERSION = "18"
  GO_VERSION = "1.19"
